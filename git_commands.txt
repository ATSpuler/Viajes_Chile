git init
# inicializar un git workspace

git add
# cargar archivos a agregar al seguimiento

git commit -m "comment"
# commitear/fijar archivos al seguimiento

git status
# muestra el estatus del _________

git clone (url)

commit -a -m "message"

git push origin main

git config --list

git log

git diff

git remote

git remote -v

git remote show

git remote rename (name) (new_name)

git remote (rm name)


